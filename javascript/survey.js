/*Build a Survey function constructor (class) that Will recieve a question
object.*/

function Survey(Obj){
   //take in an array of question objects
      // - this will have the answers in them
   //take in a title
   //take in a description of the survey
}

//METHODS of the Survey constructor
//Display the title
//display the description
//display the questions with their answers and name(key) attribute;
function Question(qNum,q,numAn,a) {
  this.questionNumber = numq;
  this.question = q;
  this.numberOfAnswers = numAn;
  this.a[] = ;
}
a = array()
q = new Question(1,"hello world",3,)
console.log()
